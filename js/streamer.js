/* ==========================================================
   FUTURYCRAFT - CANDIDATURA STREAMER
========================================================== */
document.addEventListener("DOMContentLoaded", function () {
    if (typeof CriadorForm === "function") {
        window.criadorStreamer = new CriadorForm("streamer");
    }
});